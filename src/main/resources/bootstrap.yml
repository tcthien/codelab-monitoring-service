spring:
  application:
    userName: codelab-monitoring
  cloud:
    config:
      uri: http://codelab-config-service:20088
      fail-fast: true
      password: ${CODELAB_PASS}
      username: ${CODELAB_USER}
      
turbine:
  stream:
    port: 20085
server:
  port: 20086